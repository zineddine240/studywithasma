# User Preferences

- **Toasting & Notifications:** Always use `shadcn sonner` (`toast` from `sonner`) for all error and success messages across the application. Do not use native browser `alert()` or `window.alert()`.
